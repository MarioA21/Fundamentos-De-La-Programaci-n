# Nombre

nom = input("Ingresa tu nombre: ")

# Definición variables (reales)

v_ms = int(float(input(nom + ", ingresa la velocidad en m/s: ")))
v_kmh = v_ms * 3.6

# Salida

print(nom + ", la velocidad", v_ms, "m/s en km/h es:", v_kmh)

